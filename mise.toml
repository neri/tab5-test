[tasks.run]
description = "Run the application"
run = "make run"

[tasks.clean]
confirm = "Are you sure?"
description = "Clean the build artifacts"
run = "make clean"

[tasks.update]
description = "Update the dependencies"
run = "make update"

[tasks.test]
description = "Run the host-side tests of the browser, font codec, fonts, spki, time and system UI crates"
# Explicitly host-targeted: `.cargo/config.toml` points the default target at
# `riscv32imafc-unknown-none-elf`, where there is no test harness to run. Only
# `tab5-browser`, `tab5-font-codec`, `tab5-font`, `tab5-ui-font`, `tab5-spki`,
# `tab5-time` and `tab5-system-ui` build for the host;
# the firmware package does not.
run = "make test"

# [tasks.doc]
# description = "Generate documentation"
# run = "make doc"

[tasks.build]
description = "Build the application"
run = "make build"

[tasks.monitor]
description = "Run a monitor for the application"
run = "python3 tools/monitor.py"

[tasks.dev]
description = "Run the application in development mode"
run = "mise run run && mise run monitor"
