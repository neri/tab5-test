[package]
name = "tab5-font"
version = "0.1.0"
edition = "2024"
publish = false

# No dependencies, on purpose. This crate is a lookup over a byte array that
# `tools/font/generate.py` produced ahead of time, which is what lets the
# firmware link it without a parser and lets `cargo test` build it for the
# host. See `docs/FONT_MIGRATION_PLAN.md`.
[dependencies]
