# tab5test

M5Stack Tab5（ESP32-P4 ECO2）のベアメタルRust実験用リポジトリです。
設計、ハードウェア構成、ECO2固有の制約は[DESIGN.md](DESIGN.md)を参照してください。

## ドキュメントの扱い

**`README.md`は人間がメンテします。指示された場合を除き編集しないでください。**
コードの追加・削除でREADMEの記述が古くなる場合も、勝手に直さずに「READMEの
この記述が実態と合わなくなる」と報告するだけにとどめ、更新するかは人間が
決めます。`.claude/settings.json`の`permissions.ask`でも確認を挟むように
設定してあります。

**`README.md`は技術的な詳細を書く場所ではありません。**「何ができるか」を
短く伝えるところで、実装の仕組み、レジスタやアドレス、定数と上限、キー割り
当ての一覧、失敗時の挙動、設計上の判断は`DESIGN.md`と`docs/`以下に書き、
READMEからはリンクするだけにします。同じ内容が2箇所にあると、必ず片方が
古くなります。

編集を指示されたときも、記述を足す前に「これは`docs/`に書くべきではないか」を
確かめてください。READMEが実装と合わなくなったと報告するときも、詳細を
書き直す案ではなく、**その記述を削って`docs/`へ委ねる案**を第一候補として
出します。

`DESIGN.md`と`docs/`以下は通常の作業対象です。実装を変えたら対応する記述も
更新してください。

設計資料の入口は`DESIGN.md`です。まず同ファイルの「ドキュメント構成」の表から、
作業対象に対応する`docs/`以下の文書を選んでください。`docs/`のうち名前が
`*_PLAN.md`ではない文書が実装の現状を説明する文書で、実装を変えたときに更新する
のはこちらです。`*_PLAN.md`は機能追加時の段階分けと実機での判断記録なので、
未完了と明記されたStageを除き、現在の実装仕様は現状文書とコードを優先します。
リポジトリ全体の作業規則は[AGENTS.md](AGENTS.md)にあります。

## 言語

- `src/`以下のコードコメント（`//`・`///`・`//!`）はすべて英語
- `DESIGN.md`・`README.md`など人間向けドキュメントは日本語

## ビルド

```sh
cargo build --release
```

`riscv32imafc-unknown-none-elf`をターゲットにした`no_std`バイナリです。
debugビルドはRAMに収まらずリンクに失敗するので、確認は必ず`--release`で
行ってください。実機への書き込みは`cargo run --release`（`espflash`）ですが、
これは実機を触れる人間が行うコマンドです（下記）。

## 動作確認は人間に依頼する

このリポジトリのコードはM5Stack Tab5実機の上でしか動きません。エージェントが
自分で確認できるのは`cargo build --release`が通ることと、必要ならホスト側で動く
テストまでです。**書き込み・実行・シリアル出力の確認・USB機器やSDカードを使った
試験は、実機とその周辺機器を触れる人間にしかできません。**`cargo run --release`
（書き込みを伴う）をエージェントが実行してはいけません。

実装や修正を終えたら、動いたことにせず、次を最終報告に書いて人間に確認を依頼して
ください。

- 何を変更したか、どの挙動が変わるはずか
- 実機で確認してほしい手順（書き込み後に叩くコマンド、必要な周辺機器、接続構成）
- 期待する出力と、失敗しているときに見えるはずの症状

実機で確認できていないものを「確認済み」「動作します」と書かないでください。
`docs/`や`*_PLAN.md`に実機確認の結果を書き足すのは、人間から確認結果を伝えられた
後です。

## コーディング方針

各モジュール末尾の`read`/`write`/`modify`（任意の`usize`アドレスを読み書きする
MMIOプリミティブ）は`unsafe fn`として定義します。これらを呼び出す関数は、既知の
ハードウェア定数アドレスしか渡さないことで安全性を担保するので`unsafe fn`には
せず、関数内で`unsafe { ... }`ブロックにまとめます（呼び出し1つずつを`unsafe`で
囲むのではなく、関数単位でまとめる）。
