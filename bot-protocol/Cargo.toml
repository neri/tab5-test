[package]
name = "tab5-bot-protocol"
version = "0.1.0"
edition = "2024"
publish = false

# No dependencies: this crate only parses the 13-byte BOT Command Status
# Wrapper and compares its residue with host-observed transfer lengths.
[dependencies]
