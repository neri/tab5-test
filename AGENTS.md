# リポジトリ作業規則

## 設計資料の入口

- 設計資料の入口は`DESIGN.md`です。まず同ファイルの「ドキュメント構成」から、
  作業対象に対応する`docs/`以下の文書を選んでください。
- `DESIGN.md`と`docs/`直下の文書は実装の現状を説明します。実装を変更した場合は、
  対応する現状文書も同じ作業で更新してください。
- `docs/plans/`以下は機能追加時の段階分けと実機での判断記録（作業計画）で、一覧は
  `docs/plans/INDEX.md`です。状態ごとに`active/`（着手済み・受入待ち）、`proposed/`
  （未着手）、`archive/`（完了・凍結・打ち切り）へ分けます。未完了と明記されたStageを
  除き、現在の実装仕様は現状文書とコードを優先してください。`archive/`は当時の記録で、
  現状と食い違う記述を含みます。
- 計画の状態が変わったら`git mv`で該当ディレクトリへ移し、`docs/plans/INDEX.md`と、
  そのファイルを指すリンク・パス（`docs/`、`src/`、`Cargo.toml`、`tools/`など）を同じ
  作業で直してください。
- 現状文書を追加したら`DESIGN.md`の「ドキュメント構成」の表に、計画を追加したら
  `docs/plans/INDEX.md`に必ず1行足してください。表に載っていない文書は、他の作業で
  見つけてもらえません。
- 新しい文書は1行目を`# 見出し`にし、その下に索引（`DESIGN.md`への相対リンク。
  `docs/`直下なら`../DESIGN.md`、`docs/plans/*/`なら`../../../DESIGN.md`）を
  引用行で置いてください。書式は既存の文書に合わせます。

## 動作確認は実機が要る——人間に依頼する

- このリポジトリのコードはM5Stack Tab5実機の上でしか動きません。エージェントが自分で
  確認できるのは`cargo build --release`が通ることまでです。
- 書き込み、実行、シリアル出力の確認、USB機器やSDカード、ディスプレイ、タッチを使う
  試験は、実機とその周辺機器を触れる人間にしかできません。
- `cargo run --release`（書き込みを伴う）や`espflash`をエージェントが実行しては
  いけません。ビルドの確認は`cargo build --release`で行ってください。
- 実機で確認していない挙動を「確認済み」「動作します」と書いてはいけません。
  ビルドが通っただけの状態は「未確認」です。
- 実装や修正を終えたら、最終報告に次を書いて人間に確認を依頼してください。
  - 何を変更したか、どの挙動が変わるはずか
  - 実機で確認してほしい手順（書き込み後に叩くコマンド、必要な周辺機器、接続構成）
  - 期待する出力と、失敗しているときに見えるはずの症状
- `docs/`や`docs/plans/`に実機確認の結果（受入済み、○回連続成功など）を書き足すのは、
  人間から確認結果を伝えられた後です。エージェントの推測で書かないでください。

## README.mdは人間が管理する

- `README.md`は人間が管理するファイルです。
- ユーザーが現在の依頼で`README.md`の変更を明示的に指示した場合を除き、作成、編集、
  整形、移動、名前変更、削除をしてはいけません。
- 「ドキュメントを更新する」「ドキュメントを実装と同期する」「作業を完成させる」などの
  一般的な依頼は、`README.md`を変更する許可にはなりません。
- 実装変更によって`README.md`の記述が古くなる場合は、変更せず、最終報告で該当箇所と
  不一致の内容を具体的に報告してください。
- 作業終了前に、この作業によって`README.md`が変更されていないことを確認してください。
- 作業開始前から存在する`README.md`の差分はユーザーの所有物です。変更、整形、復元を
  せず、そのまま残してください。

## README.mdは簡潔に保つ

- `README.md`は「何ができるか」を短く伝える場所であり、**技術的な詳細を書く場所では
  ありません**。実装の仕組み、レジスタやアドレス、定数と上限、キー割り当ての一覧、
  失敗時の挙動、設計上の判断は`DESIGN.md`と`docs/`以下に書きます。
- READMEには結論と`docs/`へのリンクだけを置きます。同じ内容を2箇所に書くと、片方が
  必ず古くなります——実際、機能を追加するたびに古くなっていたのはREADMEの説明が
  長い行でした。
- 変更を指示されたときは、記述を足す前に「これは`docs/`に書くべきではないか」を確かめて
  ください。足す提案より短くする提案を優先します。
- 実装変更でREADMEの記述が古くなったと報告するときは、詳細を書き直す案ではなく、
  その記述を削って`docs/`へ委ねる案を第一候補として示してください。
- **この規則は上の「人間が管理する」規則を置き換えません。**簡潔にするための編集も、
  現在の依頼で明示的に指示された場合を除いて行いません。
