# リポジトリ作業規則

## 設計資料の入口

- 設計資料の入口は`DESIGN.md`です。まず同ファイルの「ドキュメント構成」から、
  作業対象に対応する`docs/`以下の文書を選んでください。
- `DESIGN.md`と、名前が`*_PLAN.md`ではない`docs/`以下の文書は実装の現状を説明します。
  実装を変更した場合は、対応する現状文書も同じ作業で更新してください。
- `*_PLAN.md`は機能追加時の段階分けと実機での判断記録です。未完了と明記されたStageを
  除き、現在の実装仕様は現状文書とコードを優先してください。
- `docs/`に文書を追加したら、`DESIGN.md`の「ドキュメント構成」の表（現状文書）または
  その下の作業計画リストに必ず1行足してください。表に載っていない文書は、他の作業で
  見つけてもらえません。
- 新しい文書は1行目を`# 見出し`にし、その下に索引（`../DESIGN.md`へのリンク）を
  引用行で置いてください。書式は既存の`docs/`の文書に合わせます。

## README.mdは人間が管理する

- `README.md`は人間が管理するファイルです。
- ユーザーが現在の依頼で`README.md`の変更を明示的に指示した場合を除き、作成、編集、
  整形、移動、名前変更、削除をしてはいけません。
- 「ドキュメントを更新する」「ドキュメントを実装と同期する」「作業を完成させる」などの
  一般的な依頼は、`README.md`を変更する許可にはなりません。
- 実装変更によって`README.md`の記述が古くなる場合は、変更せず、最終報告で該当箇所と
  不一致の内容を具体的に報告してください。
- 作業終了前に、この作業によって`README.md`が変更されていないことを確認してください。
- 作業開始前から存在する`README.md`の差分はユーザーの所有物です。変更、整形、復元を
  せず、そのまま残してください。
